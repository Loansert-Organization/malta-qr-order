name: Cursor Guard

on:
  push:
    branches:
      - main
      - lovable/**

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Cursor CLI
        run: npm install -g cursor-cli@latest
      - name: Run Cursor robot
        run: |
          cursor-cli review \
            --openai-key ${{ secrets.OPENAI_API_KEY }}